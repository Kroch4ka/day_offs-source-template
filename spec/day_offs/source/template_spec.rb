# frozen_string_literal: true

RSpec.describe DayOffs::Source::Template do
  it "has a version number" do
    expect(DayOffs::Source::Template::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
